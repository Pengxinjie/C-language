//存储类别，链接和内存管理/*涉及到的关键字：1.auto2.extern3.static4.register5.const6.volatile7.restricted8._Thread_local9._Atomic*//*涉及到的函数1.rand()2.srand()3.time()4.malloc()5.calloc()6.free()*/#include<stdio.h>#include<stdlib.h>#include<malloc.h>#include<string.h>extern int a = 1;//定义一个全局变量int main(void){	/*链接	c变量的三个链接属性：外部链接；内部链接和无链接	外部链接变量可以在多文件程序中使用；	#全局作用域	内部链接变量只能在一个翻译单元中使用；  #文件作用域	自动变量：块代码执行时存储，结束后释放-->这些变量属于定义它们的块，函数或原型所有；	auto 存储类说明符  eg. auto int plox;	寄存器变量：储存在最快的可用内存中；	eg. register int a；未必申请得到；是一种请求，申请失败就变成自动变量；	注意：变成自动变量后，仍然不可以使用&运算符！！！！！！！	可声明为register的数据类型有限，寄存器空间不足以存储double类型的变量；	不能在函数形参中使用static！！	外部链接的静态变量：eg. extern int a；	如果在一个源代码文件中使用的外部变量定义在另一个源代码中，则必须使用extern；	外部变量初始化注意点：只能使用常量表达式初始化文件作用域变量；	eg.int x2=2*x；  错，x为变量；	若未初始化外部变量，则会被自动初始化成0；	*/	int* p = (int*)malloc(sizeof(20));	int i;	memset(p, 0, 20);//为每一个字节赋值的函数；  headfile <string.h>or<memory.h>	for (i = 0; i < 4; i++)	{		printf("%d\n", p[i]);	}	//malloc出来的空间全被赋值成0；	free(p);//释放指针--->空指针	p = NULL;//赋值成NULL	if (p == NULL)	{		printf("操作到无效指针！");//提示，防止指针操作非法空间！	}	system("pause");	return 0;}